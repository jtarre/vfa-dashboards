label.control-label Related To
select#relatedToList.chosen-select.form-control(data-placeholder="Choose Case (if applicable)")
	each id, name in caseList
		option(id=id) #{name}
script.
	$("select").chosen({ width: '100%' });