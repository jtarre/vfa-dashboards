describe("companyCtrl", function() {

	describe("meta fields", function() {
		it("should exist");
		it("should have specific fields");
		it("should attach itself to appropriate scope variable");
		it("should have data");
		it("shouldn't crash program if it doesn't exist");
		// etc.

	});

	describe("company data", function() {
		it("should exist");
		it("if it doesn't, should not crash system");
		it("should be stored in a scope variable");
		// etc.
	})
});