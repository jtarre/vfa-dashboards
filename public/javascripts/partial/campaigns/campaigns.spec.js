describe("companyCtrl", function() {
	describe("form fill", function() {
		it("should have all fields filled in");
		it("should throw error if fields aren't found");
		it("should make an api call on button click");
	})
})